# 2026-05-20

- Rodrigo asked me to configure the local Guatecompras tooling to query road/infrastructure projects in licitación with estado vigente from May 1 onward.
- Updated `consultar_db.py` defaults to filter `estado=vigente`, `desde=2026-05-01`, and road/infrastructure keywords.
- Updated `inicializar_db.py` schema to include `fecha_publicacion`, `estado`, and `modalidad`, and removed the hard dependency on `requests`.
- Updated `TOOLS.md` so future reports use `python consultar_db.py --limite 5` as the default vigente-from-May-1 query.
- Rodrigo correctly challenged whether the issue was the portal or our tooling. Verification showed the tool used the wrong assumed URL; the official OCDS API works via `/files` and `/release/search`. Corrected `inicializar_db.py` to use `/release/search` and loaded 199 real vigente road/infrastructure records from May 2026.
- Rodrigo narrowed the operating scope: future Guatecompras project queries should focus only on Unidad Ejecutora de Conservación Vial, Dirección General de Caminos, and Fondo Social de Solidaridad, and the default report should show only execution-of-obra records (`mainProcurementCategory = works`). Services, supplies, machinery maintenance, fuel, and similar non-works records should be excluded unless he asks otherwise. Keep the local DB capable of storing target-entity records so excluded items can be audited.
- Rodrigo wants project opportunity rankings by physical magnitude, especially ordered from largest to smallest in square meters, to evaluate which opportunities are more convenient.
- Rodrigo requested a daily search for vigente road-related projects in these purchasing units: Unidad Ejecutora de Conservación Vial, Compras DGC, and Fondo Social de Solidaridad. The daily output should rank convenience by detected obra volume, from larger to smaller. Persisted criteria in `GUATECOMPRAS_CRITERIOS.md`.
- Rodrigo clarified that the projects of interest must be Licitación Pública only. Exclude cotización, compra directa, cases of exception, subasta, convenio, contratación directa, and other modalities unless he explicitly asks otherwise.
- Refactored `consultar_db.py` output: replaced raw JSON dump with `construir_reporte()` (Telegram-ready Markdown: NOG bold, monto en Q, fechas, link). Added `--json` flag to keep structured output available for the agent when needed.
- Test run revealed data-quality issue: current DB has only 13 records, all classified as `mainProcurementCategory = services` (0 `works`). The 6 Licitación Pública rows are all SG-001/002/003 SEÑALIZACIÓN — correctly excluded by criteria. `tender.value.amount` is Q0.00 across the board. Next step likely: re-run `inicializar_db.py` to refresh, and/or revisit whether OCDS categoría `works` is reliable in Guatecompras or if we should infer from keywords + entidad instead.
- Probed OCDS directly: 13 of the first 20 records on page 1 are `works + Licitación Pública vigente`, but all from MUNICIPALIDADES (San Pedro Carchá, San Juan Sacatepéquez, La Blanca, Antigua Guatemala). Confirmed Rodrigo's hypothesis that the entity restriction was the bottleneck, not the data.
- Rodrigo chose "ampliar + marcar prioridad" approach: ingest now stores ALL works + LP vigente that match road keywords, tagged with `prioridad` (alta=UECV/DGC/FSS, media=municipalidades, baja=otros). Default report shows alta+media; `--prioridad alta` enfoca solo entidades objetivo.
- Refactored `inicializar_db.py`: removed pre-filter by entity, added `detectar_prioridad()`, `es_obra_vial()`, `es_licitacion_publica()` helpers. Added `prioridad` column + migration to schema.
- Refactored `consultar_db.py`: replaced `--todas-las-entidades`/`--incluir-servicios`/`--solo-viales`/`--todas-las-modalidades` (all redundant now that ingest enforces) with single `--prioridad alta|media|baja|todas|csv` flag. Report shows badge per row (🟢 ALTA / 🟡 MEDIA / ⚪ BAJA).
- Refresh result: 42 obras viales LP vigentes desde 2026-05-01. 0 alta, 42 media, 0 baja. CIV/DGC/FSS no han publicado obras directas en este periodo; toda la obra vial vigente está en municipalidades. `tender.value.amount` sigue viniendo Q0.00 en todos — Guatecompras no publica monto referencial en OCDS.
- Updated TOOLS.md y GUATECOMPRAS_CRITERIOS.md para reflejar el modelo de prioridades.
- Rodrigo confirmó que 0 NOGs vigentes de UECV/DGC/FSS desde mayo es real, no bug. Avanzamos al frente de extracción de volumen físico.
- Decisión clave: usar el modelo GPT autenticado vía OpenClaw onboard (no API key directa). El CLI `openclaw capability model run --prompt "..." --json` invoca `openai/gpt-5.5` autenticado por Codex OAuth y devuelve JSON con `outputs[0].text`.
- Adopté `.venv/bin/python` como intérprete oficial del workspace (Python 3.9.6 con requests, pdfplumber, pdfminer.six). Documentado en TOOLS.md. Claudio nunca debe usar `python3` global de Homebrew.
- Creado `extraer_volumen.py`: baja PDFs relevantes (bases, diseño, especificaciones) → texto vía pdfplumber → prompt estructurado a `openclaw capability model run` → persiste m2_total/ml_total/m3_total/resumen_alcance/renglones_clave/confianza en `concursos`. PDFs se cachean en `pdfs_cache/{NOG}/`. Idempotente vía `fecha_extraccion`.
- Esquema ampliado: nuevas columnas `m2_total REAL`, `ml_total REAL`, `m3_total REAL`, `resumen_alcance TEXT`, `confianza_volumen TEXT`, `renglones_json TEXT`, `fecha_extraccion TEXT`, `notas_extraccion TEXT`. Migración automática en `asegurar_columnas`.
- `consultar_db.py` re-rankea: ranking primario `m2_total DESC` con NULLs al final; secundario monto, terciario prioridad. Reporte muestra m² destacado, alcance en una línea, magnitudes ml/m³, badge de confianza si no es alta. Para NOGs sin volumen, muestra "volumen pendiente".
- POC validado con NOG 30098408 (camino rural pavimento rígido San Pedro Carchá, Alta Verapaz): extracción end-to-end exitosa. Resultado: m²=18,908.6 / ml=6,643 / m³=4,216.64 / confianza=alta. Renglones detallados (1,100 ml de camino, 5,499.5 m² de pavimento rígido, 2,219 ml cunetas, 24 ml drenaje, 116 m³ muro de contención). Tiempo: ~3 minutos por NOG.
- Pendiente operativo: Rodrigo correrá `.venv/bin/python extraer_volumen.py --todos` cuando quiera completar los 41 NOGs restantes. No se procesaron en batch hoy por decisión del usuario.
- Rodrigo describió el flujo end-to-end del agente: Telegram → listado por m² → seleccionar NOGs → ficha de bases con fechas → generar oferta. Para la fase de generación de oferta SE QUEDA EN ESPERA: Rodrigo tiene PDFs (LCE, decretos, ofertas históricas exitosas) pero no a mano. Acordamos no hacer nada en ese frente hasta que él los proporcione. No iniciar `generar_oferta.py` ni asumir estructura de oferta sin esos contextos.
- Decisión de UI del listado: SOLO m² visible; ignorar m³ y ml en la lista resumen. Fechas/lugar/plazos/etc. solo se muestran en la ficha detalle on-demand.
- Ampliación del extractor: prompt extendido para sacar fechas (visita técnica + lugar, recepción ofertas, apertura plicas, consultas), plazos (ejecución, vigencia oferta, garantía obra), garantías requeridas (sostenimiento, cumplimiento, anticipo, conservación, otros con %, monto y notas) y requisitos del oferente (RGAE, capacidad financiera, profesional residente, etc.). Esquema ampliado con columnas correspondientes.
- Nuevo comando: `.venv/bin/python consultar_db.py --nog NNNN` produce ficha detalle estructurada por secciones (📐 alcance, 📅 fechas, ⏱ plazos, 🛡 garantías, 📋 requisitos, 📊 renglones). Si el NOG no tiene extracción aún, sugiere correr el extractor.
- POC validado de extractor extendido sobre NOG 30098408: extrajo lugar de visita técnica (dirección municipal completa), 6 garantías con %/monto/notas, 8 requisitos clave, plazos completos (210 días ejecución, 120 días vigencia oferta, 18 meses garantía obra), 12 renglones de obra con cantidades. La fecha de visita venía con placeholder "XX DE XXXXX DEL 2,026 XX:XX" en las bases publicadas — el modelo lo identificó y bajó confianza a "media" correctamente. Esto es señal real de que la municipalidad aún no ha fijado fecha y hay que llamar para confirmar.
- Creados scripts arrancar_claudio.sh y apagar_claudio.sh + apps de Automator en Escritorio ("Despertar a Claudio", "Apagar a Claudio"). Primera iteración tenía bug en PATH (faltaba /usr/sbin donde vive lsof) — corregido.
- DIAGNÓSTICO ARQUITECTÓNICO IMPORTANTE: OpenClaw ya tenía un LaunchAgent oficial macOS gestionando el gateway: `~/Library/LaunchAgents/ai.openclaw.gateway.plist` con Label=`ai.openclaw.gateway`, KeepAlive=true, RunAtLoad=true, puerto=18789. Esa instancia (PID 703 ese día) arranca automáticamente al login, se reinicia si crashea, y es la que conversa con Telegram (@LicitadorClaudioBot). Mi primera versión de los scripts arrancaba un SEGUNDO gateway redundante en puerto 29000 que entraba en guerra de polling con el oficial (error 409 "Conflict: terminated by other getUpdates request" en loop con backoff exponencial). El "Apagar" solo mataba al zombie del 29000, NO al gateway real.
- Corrección final: los scripts ahora usan `launchctl bootstrap/bootout/kickstart` para controlar el LaunchAgent oficial en lugar de spawnear un gateway paralelo. Dominio: `gui/$(id -u)`. Servicio: `gui/501/ai.openclaw.gateway`. Puerto: 18789 (el real). Despertar = kickstart si cargado, bootstrap si descargado. Apagar = bootout (detiene proceso y bloquea KeepAlive; el agent se recarga solo en el próximo login).
- Aprendizaje para futuras decisiones sobre infraestructura OpenClaw: SIEMPRE chequear primero `launchctl list | grep openclaw` y `~/Library/LaunchAgents/ai.openclaw.*` antes de proponer "scripts que arranquen openclaw". El framework ya viene con su propio servicio macOS nativo; los scripts custom deben controlarlo, no duplicarlo.
- Claudio (agente Telegram) hizo su primera auto-mejora útil: detectó que `inicializar_db.py` no tenía `--help` (al pedirle ayuda empezaba a refrescar) y que el `DELETE FROM concursos` del refresco borraba las extracciones de `extraer_volumen.py`. Implementó `snapshot_extracciones()` / `restaurar_extracciones()` + `COLUMNAS_EXTRACCION` global, y agregó `argparse.parse_args()` para soportar `--help`. Test in-memory validado: tras DELETE + reinsert con data fresca, el m²/fechas/garantías sobreviven correctamente; los NOGs que ya no están en el OCDS se descartan limpio.
- Falso positivo reportado por Claudio sobre "log de Telegram sigue mostrando conflict": eran líneas históricas del log de antes del fix de los gateways duplicados. Verificación cuantitativa muestra cero `Conflict` en los últimos 5 minutos y tres `outbound send ok` consecutivos. Lección a internalizar para el agente: cuando reporte un problema basado en logs, filtrar por timestamp reciente para no confundir hallazgos viejos con estado actual.
- Claudio procesó 5 NOGs antes de la reorganización: 30098408, 30102480, 30224462, 30235715, 30272130. De esos, 3 quedaron con m² extraído (3/3 en reporte de listado), los otros 2 con confianza baja o m² null.
- REORGANIZACIÓN COMPLETA DEL WORKSPACE: estructura plana → carpetas por responsabilidad. Raíz preserva convenciones OpenClaw (AGENTS.md, SOUL.md, USER.md, IDENTITY.md, HEARTBEAT.md, TOOLS.md, memory/). Carpetas nuevas: `tools/` (3 scripts Python), `bin/` (2 scripts shell), `data/` (DB + pdfs_cache + log), `docs/` (GUATECOMPRAS_CRITERIOS.md), `referencias/` (futuro: LCE/decretos), `ofertas_historicas/` (futuro: ejemplos exitosos).
- Refactor de paths Python: los 3 scripts ahora resuelven WORKSPACE_DIR vía `Path(__file__).resolve().parent.parent`, así DB_PATH y CACHE_DIR apuntan siempre a `data/` sin importar el cwd desde el que se invoquen. Validado corriendo `consultar_db.py` desde /tmp con éxito.
- Apps de Automator actualizadas editando directamente el `document.wflow` con PlistBuddy. Ahora apuntan a `/Users/rodrigo/.openclaw/workspace/bin/{arrancar,apagar}_claudio.sh`. No fue necesario reabrir Automator.
- Smoke test post-reorganización: py_compile OK ×3, consultar_db OK desde 2 cwds distintos, arrancar_claudio.sh desde nueva ubicación arranca Claudio limpio (PID 9089, :18789, Telegram conectando sin Conflict).
- Estado final tras reorganización: 42 NOGs en DB, 5 con extracción (3 con m² visible), Claudio despierto y operativo. Quedan 37 NOGs pendientes de extracción cuando Rodrigo lo pida a Claudio.
