# 2026-05-20

- Rodrigo asked me to configure the local Guatecompras tooling to query road/infrastructure projects in licitación with estado vigente from May 1 onward.
- Updated `consultar_db.py` defaults to filter `estado=vigente`, `desde=2026-05-01`, and road/infrastructure keywords.
- Updated `inicializar_db.py` schema to include `fecha_publicacion`, `estado`, and `modalidad`, and removed the hard dependency on `requests`.
- Updated `TOOLS.md` so future reports use `python consultar_db.py --limite 5` as the default vigente-from-May-1 query.
- Rodrigo correctly challenged whether the issue was the portal or our tooling. Verification showed the tool used the wrong assumed URL; the official OCDS API works via `/files` and `/release/search`. Corrected `inicializar_db.py` to use `/release/search` and loaded 199 real vigente road/infrastructure records from May 2026.
- Rodrigo narrowed the operating scope: future Guatecompras project queries should focus only on Unidad Ejecutora de Conservación Vial, Dirección General de Caminos, and Fondo Social de Solidaridad, and the default report should show only execution-of-obra records (`mainProcurementCategory = works`). Services, supplies, machinery maintenance, fuel, and similar non-works records should be excluded unless he asks otherwise. Keep the local DB capable of storing target-entity records so excluded items can be audited.
- Rodrigo wants project opportunity rankings by physical magnitude, especially ordered from largest to smallest in square meters, to evaluate which opportunities are more convenient.
- Rodrigo requested a daily search for vigente road-related projects in these purchasing units: Unidad Ejecutora de Conservación Vial, Compras DGC, and Fondo Social de Solidaridad. The daily output should rank convenience by detected obra volume, from larger to smaller. Persisted criteria in `GUATECOMPRAS_CRITERIOS.md`.
